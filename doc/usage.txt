What is it
----------

Tasked is kind of a strange web server with read/write access to static files, PAM authentication, proxied cascading and
publish/subscribe notifications. A nice behaving building block for custom networking scenarios.

Running
-------

HTTP API
--------

Options
--------

### Evaluation rules of config, environment and flags.

### TLS keys

Path to a file.
Pem format.


### AES keys

No line break at the end.


Syncing
-------

Have the clock configured, because syncing deleted files can trick you.
